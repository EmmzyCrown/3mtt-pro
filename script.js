function getAIResponse() { 
    const userText = 
    document.getElementById("userInput").
    value; 
    const responseBox = 
    document.getElementById("response"); 

    // Simulated AI logic (basic keywords)
    //  if (userText.toLowerCase().includes("hello")) { 
    // responseBox.innerText = "Hi there! How can I help you?"; 
    // } else if (userText.toLowerCase().includes("weather")) 
    // { responseBox.innerText = "It looks sunny today!"; 
    // } else 
    // { responseBox.innerText = "I'm not sure how to respond to that."; }
     } 

